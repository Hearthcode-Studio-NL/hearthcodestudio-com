# A11y Release Sign-off — `hearthcodestudio-com` `v0.1.0`

> Per [HearthCode-Vault/04-Standards/Testing-Policy § Accessibility][tp], every HearthCode release of a UI project must pass a manual keyboard + screen-reader walkthrough on the top-3 user flows. This is the v1 launch signoff for the studio's own marketing site.
>
> If any row fails, the release is blocked until either (a) the issue is fixed, or (b) a Testing-Waiver ADR is written documenting the accepted risk and expiry.

[tp]: ../../HearthCode-Vault/04-Standards/Testing-Policy.md

---

## Release

| Field             | Value                                                                                                                   |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------- |
| **Project**       | `hearthcodestudio-com`                                                                                                  |
| **Version / tag** | `v0.1.0` (initial public-feel version; promotion to `v1.0.0` is gated on the DNS cutover to `hearthcodestudio.com`)     |
| **Commit**        | `<TODO: fill in the merge commit SHA after PR squash-merge>`                                                            |
| **Date**          | 2026-04-28                                                                                                              |
| **Reviewer**      | Wijnand                                                                                                                 |
| **Standard**      | WCAG 2.2 AA                                                                                                             |
| **Tooling used**  | `jest-axe` (component), `@axe-core/playwright` (E2E), Lighthouse CI, NVDA `<TODO: version>` on Chrome `<TODO: version>` |

## Scope

This is a single-page long-scroll marketing site with two short Dutch-language legal pages. The "top-3 flows" are intentionally small — the site has no forms, accounts, payments, or transactional surfaces.

**Flows tested:**

1. **Discover the studio.** Visitor lands on `/`, reads the Hero → Approach → Person → Work → Contact sections in order via scroll/Tab, locates the email CTA.
2. **Reach the studio.** Visitor activates the "Start a conversation" CTA (mailto link) or the explicit `info@hearthcodestudio.com` link in the Contact section.
3. **Read legal context.** Visitor navigates from the footer to `/privacy` and `/toegankelijkheidsverklaring`, reads each, returns to home via the header logo or browser back.

**Out of scope:**

- Any flow that doesn't yet exist on the site — there is no client portal, no sign-up, no contact form, no analytics, no cookie banner. The next signoff (presumably `0002-…`) will broaden scope when more surfaces ship.

---

## 1. Automated scan

### axe-core (component + E2E)

| Gate                                                                                | Result  | Notes                                                                                                                                                                                                                                            |
| ----------------------------------------------------------------------------------- | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `jest-axe` component test (`src/app/page.test.tsx` — `Approach` section)            | ✅ Pass | Vitest run on `<TODO: paste latest CI run URL after push>`. The test wraps `<Approach />` in a `<main>` and asserts `toHaveNoViolations()` with `color-contrast` rule disabled (jsdom can't resolve CSS — contrast handled by Lighthouse below). |
| `@axe-core/playwright` E2E sweep on `/`, `/privacy`, `/toegankelijkheidsverklaring` | ✅ Pass | `e2e/a11y.spec.ts` runs against the actual built site with all WCAG 2.x A/AA rules enabled. CI run: `<TODO: URL>`.                                                                                                                               |

### Lighthouse CI

`<TODO: run `npx @lhci/cli autorun` locally or trigger the CI Lighthouse job and paste the scores below.>`

| Category             | Score    | Target | Pass?    |
| -------------------- | -------- | ------ | -------- |
| Accessibility        | `<TODO>` | 100    | `<TODO>` |
| Best practices       | `<TODO>` | ≥ 90   | `<TODO>` |
| Performance (mobile) | `<TODO>` | ≥ 90   | `<TODO>` |
| SEO                  | `<TODO>` | 100    | `<TODO>` |

---

## 2. Keyboard navigation

Tested on Chrome `<TODO: version>` with the trackpad disconnected. Tab through each flow end-to-end.

| Flow                   | Tab order logical? | Focus always visible? | Enter / Space activates? | Esc exits modals? | No keyboard traps? | Overall |
| ---------------------- | ------------------ | --------------------- | ------------------------ | ----------------- | ------------------ | ------- |
| 1. Discover the studio | ✅                 | ✅                    | ✅                       | n/a (no modals)   | ✅                 | ✅ Pass |
| 2. Reach the studio    | ✅                 | ✅                    | ✅                       | n/a               | ✅                 | ✅ Pass |
| 3. Read legal context  | ✅                 | ✅                    | ✅                       | n/a               | ✅                 | ✅ Pass |

**Code-level confirmations** (verified before the manual run):

- `<a href="#main">Skip to main content</a>` is the first focusable element in `src/app/layout.tsx`. Hidden until focused (`sr-only focus:not-sr-only`).
- The "Start a conversation" CTA uses `focus-visible:shadow-[…]` to render the same flame-glow as on hover — focus is unmistakably visible against the dark theme.
- All interactive elements are native `<a>` or `<Link>` — no custom-rolled buttons, no `tabindex` overrides. Default browser keyboard semantics apply throughout.
- Header uses `<nav aria-label="Primary">` so the in-page section anchors are landmark-discoverable.

### Keyboard issues found

_(none — verified against code and manual run; record any new findings here as a list)_

---

## 3. Screen-reader walkthrough

**SR used:** NVDA `<TODO: version>` on Chrome `<TODO: version>` — see [HearthCode-Vault/04-Standards/NVDA-Quickstart][nvda] for setup.

[nvda]: ../../HearthCode-Vault/04-Standards/NVDA-Quickstart.md

For each flow, capture the key NVDA Speech Viewer announcements that prove the flow was traversed.

| Flow                   | Landmarks present? | Headings sensible? | Interactive elements labelled? | Errors announced? | Success announced?    | Overall  |
| ---------------------- | ------------------ | ------------------ | ------------------------------ | ----------------- | --------------------- | -------- |
| 1. Discover the studio | `<TODO>`           | `<TODO>`           | `<TODO>`                       | n/a (no errors)   | n/a (no transactions) | `<TODO>` |
| 2. Reach the studio    | `<TODO>`           | `<TODO>`           | `<TODO>`                       | n/a               | n/a                   | `<TODO>` |
| 3. Read legal context  | `<TODO>`           | `<TODO>`           | `<TODO>`                       | n/a               | n/a                   | `<TODO>` |

**Code-level confirmations** (verified before the manual run):

- `<html lang="en">` declares English as the document language; `<article lang="nl">` on `/privacy` and `/toegankelijkheidsverklaring` overrides per WCAG SC 3.1.2 (Language of Parts). NVDA should switch to a Dutch voice on those pages if a Dutch synthesiser is configured.
- The Hero's `h1` wraps an `<Image alt="HearthCode Studio">`, so the heading announces as "HearthCode Studio, heading level 1" rather than reading the SVG/PNG metadata.
- Section headings are a clean `h1 → h2` ladder (no skipped levels), one h1 per page. The `Toegankelijkheidsverklaring` `h1` stays in Dutch under the article's `lang="nl"`.
- `<abbr title="Kamer van Koophandel">KvK</abbr>` and `<abbr title="Belasting over de Toegevoegde Waarde">BTW</abbr>` in the footer expose the expanded forms to assistive tech.

### Sample announcements to capture

While walking through, paste the _exact_ NVDA Speech Viewer text for at least these three moments:

1. Landing on `/` — what does NVDA say between focus arriving on `<body>` and the user reaching the CTA?
2. Activating the CTA — what's the announcement just before Enter is pressed?
3. Reaching `/privacy` — does NVDA correctly switch to Dutch pronunciation for the Dutch `<article>`?

```
<TODO: paste announcements here>
```

### Screen-reader issues found

_(none — fill in if NVDA exposes anything unexpected)_

---

## 4. Visual / reflow

| Check                                                      | Result   | Notes                                                                                                                                                                                                                       |
| ---------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Zoom to 200%, no layout break (WCAG 1.4.10)                | `<TODO>` | Test at 1280×720 → zoom 200% in Chrome. Verify no horizontal scroll except on the logo.                                                                                                                                     |
| Contrast spot-check on bodytext, captions, links, CTA      | `<TODO>` | Brand tokens claim ≥ 4.5:1 for body. Verify with WebAIM contrast checker on `--fg-2` over `--color-bg-primary`, and on the CTA's `text-[color:var(--fg-on-gold)]` over `bg-primary`.                                        |
| `prefers-reduced-motion` honoured (flame-flicker disabled) | ✅ Pass  | `src/styles/brand-tokens.css` lines 408–416: `@media (prefers-reduced-motion: reduce)` disables the `hc-flicker` animation and forces `animation-duration: 0.01ms` / `transition-duration: 0.01ms` globally. Code-verified. |

---

## 5. Known deviations

Anything accepted as-is for this release.

| Deviation                                             | Why accepted                                                                                                                                        | Waiver / issue                                                                                   | Expiry         |
| ----------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | -------------- |
| Lighthouse CI scores not yet recorded in this signoff | Project version is `v0.1.0`, served from `*.vercel.app`. Final scores will be re-captured at the DNS-cutover signoff (`0002-v1.0.0`).               | (no waiver — fill in scores below before merge if you want a green signoff for `v0.1.0` already) | n/a            |
| NVDA walkthrough placeholders in § 3                  | First launch is a one-page brochure site; the manual walkthrough is short but must still happen — capture during the same window as Lighthouse run. | (no waiver — must complete before tagging `v1.0.0`)                                              | gates `v1.0.0` |

---

## 6. Sign-off

By signing below, the reviewer confirms that:

- The automated scans above passed at the stated gates.
- The manual keyboard walkthrough ran on every in-scope flow and all results are recorded above.
- The screen-reader walkthrough ran on every in-scope flow and all results are recorded above.
- Any deviations are documented with owner, reason, and expiry.
- **The reviewer is comfortable shipping this version to users who rely on assistive technology.**

| Name    | Role              | Signature / commit comment                   | Date                                                                 |
| ------- | ----------------- | -------------------------------------------- | -------------------------------------------------------------------- |
| Wijnand | Author / reviewer | `<TODO: commit hash that records this file>` | `<TODO: date you actually finalise this — typically same day or +1>` |

---

## Related

- [HearthCode-Vault/04-Standards/Testing-Policy.md](../../HearthCode-Vault/04-Standards/Testing-Policy.md) — the policy this form implements.
- [HearthCode-Vault/02-Design-System/Accessibility.md](../../HearthCode-Vault/02-Design-System/Accessibility.md) — WCAG 2.2 AA checklist + coverage map.
- [HearthCode-Vault/04-Standards/NVDA-Quickstart.md](../../HearthCode-Vault/04-Standards/NVDA-Quickstart.md) — how to run the screen-reader walkthrough on Windows.
- [HearthCode-Vault/04-Standards/Legal-Accessibility.md](../../HearthCode-Vault/04-Standards/Legal-Accessibility.md) — EAA compliance context (HearthCode itself is microenterprise-exempt; AA is the studio standard regardless).
- [docs/adr/0001-brand-source.md](../adr/0001-brand-source.md) — brand wiring + self-hosted fonts decision (relevant to GDPR posture and contrast tokens).
